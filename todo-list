** Current todo

* LDAP login to Solena
  * Operate as the user we are logging in as
  * Password should be encrypted using php-mcrypt
  * Assign member groups as roles
  
* RBAC
  * Database or PHP file based?
  * write${Attribute} permissions - to be used in the UI
  * reading of attributes is controlled by LDAP server (via Solena login limitations)
